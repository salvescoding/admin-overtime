<% pending_audit_logs.each do |audit|  %>
  <%= link_to  "I confirm that I did not perform any overtime for the week: #{start_date(audit)}", confirmed_audit_log_path(audit), class: "btn btn-warning btn-block", data: {  confirm: 'Are you sure? This will confirm you have not perform overtime'}, id: "confirmed_#{audit.id}" %>
<% end %>

